Export dashboard data and open the PWA:

1. Run `python dashboard/export_dashboard_data.py` to refresh dashboard data
2. Run `python serve.py` in background if not already running
3. Open the dashboard: `start http://10.0.0.160:8000/index.html`
